#!/opt/local/bin/fish

# ----------------------------------------------------------------------
# Remove litter left behind by vi
# ----------------------------------------------------------------------

. params.fish

find . -name '*~' | xargs rm
